pull下来后：

1. 安装nodejs
2. 安装hexo：`npm install -g hexo-cli`
3. 安装hexo-deployer-git： `npm install -g hexo-deployer-git --save`
4. 安装hexo-generator-searchdb：`npm install -g hexo-generator-searchdb --save`

